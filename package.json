{
  "name": "directory-engine",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check:governance": "node scripts/validate-project-governance.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250705.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.24.3"
  }
}
